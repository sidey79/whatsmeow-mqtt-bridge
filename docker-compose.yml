name: whatsmeow-mqtt-bridge

services:
  bridge:
    build: .
    volumes:
      - whatsapp-data:/data
    env_file: .env
    ports:
      - '3000:3000'
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "/bridge", "--healthcheck"]

volumes:
  whatsapp-data:
